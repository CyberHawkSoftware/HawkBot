#The node_modules folder is a bit large, don't include that either
node_modules/*
#Don't include the mp3 files
mp3/*
#Don't include config.json to avoid showing bot token/api tokens
config.json
#Don't include ffmpeg if it's stored here
ffmpeg
#Don't include test commannds
commands/embedtest.js
commands/test.js